Place your real resume PDF here as `resume.pdf` before sending the assignment.

The live follow-up flow expects:
- `assets/resume.pdf`
- `assets/architecture.svg`

If your public host serves `/assets/*`, Twilio WhatsApp can attach them from the URLs built in `.env`.
